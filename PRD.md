# PRD (Product Requirements Document)
## 웹 기반 전화번호 관리 시스템 (SAD Phonebook)

---

## 1. 개요

### 1.1 프로젝트 목적
웹 브라우저에서 접근 가능한 전화번호부 관리 시스템을 구축한다. 사용자는 별도의 로그인 없이 연락처를 등록, 수정, 삭제할 수 있다.

### 1.2 배경
전화번호 정보를 간단하게 관리할 수 있는 경량화된 웹 애플리케이션이 필요하다. 복잡한 인증 절차 없이 누구나 바로 사용할 수 있는 시스템을 목표로 한다.

---

## 2. 기술 스택

| 구분 | 기술 |
|------|------|
| 프론트엔드 | HTML / CSS / JavaScript (또는 React) |
| 백엔드/서버 | Vercel (Serverless Functions) |
| 데이터베이스 | Supabase (PostgreSQL) |
| 배포 | Vercel |

---

## 3. 기능 요구사항

### 3.1 연락처 등록 (Create)
- 사용자는 새 연락처를 추가할 수 있다.
- 필수 입력 항목: **이름**, **전화번호**
- 선택 입력 항목: **이메일**, **메모**
- 중복된 전화번호는 등록할 수 없다.
- 전화번호 형식 유효성 검사를 수행한다.

### 3.2 연락처 조회 (Read)
- 등록된 전체 연락처 목록을 표시한다.
- 이름 또는 전화번호로 검색할 수 있다.
- 목록은 이름 기준 가나다순으로 정렬된다.

### 3.3 연락처 수정 (Update)
- 기존 연락처의 이름, 전화번호, 이메일, 메모를 수정할 수 있다.
- 수정 시 동일한 유효성 검사를 적용한다.

### 3.4 연락처 삭제 (Delete)
- 연락처를 삭제할 수 있다.
- 삭제 전 확인 다이얼로그를 표시한다.

---

## 4. 비기능 요구사항

| 항목 | 요구사항 |
|------|----------|
| 인증 | 로그인 불필요, 인증 없이 모든 기능 사용 가능 |
| 접근성 | 웹 브라우저에서 접근 가능 (PC / 모바일) |
| 응답 속도 | 주요 작업(등록, 수정, 삭제) 2초 이내 응답 |
| 보안 | SQL Injection 방지, Supabase RLS(Row Level Security) 설정 |

---

## 5. 데이터 모델

### 테이블: `contacts`

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | UUID | PRIMARY KEY, DEFAULT gen_random_uuid() | 고유 식별자 |
| `name` | VARCHAR(100) | NOT NULL | 이름 |
| `phone` | VARCHAR(20) | NOT NULL, UNIQUE | 전화번호 |
| `email` | VARCHAR(200) | NULLABLE | 이메일 |
| `memo` | TEXT | NULLABLE | 메모 |
| `created_at` | TIMESTAMP | DEFAULT now() | 생성 일시 |
| `updated_at` | TIMESTAMP | DEFAULT now() | 수정 일시 |

---

## 6. 화면 구성

### 6.1 메인 페이지 (/)
- 연락처 목록 테이블
- 검색 입력창
- 새 연락처 추가 버튼

### 6.2 연락처 추가 모달 / 페이지
- 이름, 전화번호, 이메일, 메모 입력 폼
- 저장 / 취소 버튼

### 6.3 연락처 수정 모달 / 페이지
- 기존 데이터가 채워진 입력 폼
- 저장 / 취소 버튼

### 6.4 삭제 확인 다이얼로그
- "정말 삭제하시겠습니까?" 확인 메시지
- 확인 / 취소 버튼

---

## 7. API 명세

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/contacts` | 전체 연락처 목록 조회 |
| POST | `/api/contacts` | 새 연락처 등록 |
| PUT | `/api/contacts/:id` | 연락처 수정 |
| DELETE | `/api/contacts/:id` | 연락처 삭제 |

---

## 8. 범위 외 항목 (Out of Scope)

- 사용자 인증 / 로그인
- 연락처 그룹 분류
- 연락처 가져오기 / 내보내기 (CSV, vCard 등)
- 즐겨찾기 기능
- 다국어 지원

---

## 9. 마일스톤

| 단계 | 내용 |
|------|------|
| 1단계 | Supabase 프로젝트 생성 및 테이블 설계 |
| 2단계 | API 엔드포인트 구현 (Vercel Serverless Functions) |
| 3단계 | 프론트엔드 UI 구현 (목록, 추가, 수정, 삭제) |
| 4단계 | Vercel 배포 및 통합 테스트 |
